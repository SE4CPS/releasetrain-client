{
  "name": "releasetrain-client",
  "version": "3.24.0",
  "description": "Static browser client for Releasetrain",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "http-server src -c-1 --log-ip",
    "build": "node scripts/build.js",
    "start": "http-server dist -c-1 --log-ip",
    "prod": "npm run build && npm start",
    "lint": "biome check scripts tests",
    "format": "biome check --write scripts tests",
    "test": "npm run build && playwright test",
    "check": "npm run build && npm run lint && playwright test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SE4CPS/releasetrain-client.git"
  },
  "bugs": {
    "url": "https://github.com/SE4CPS/releasetrain-client/issues"
  },
  "keywords": [
    "release",
    "versioning",
    "tracking",
    "client"
  ],
  "author": "berhes",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.10",
    "@playwright/test": "^1.48.0"
  }
}
